<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Notepad" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.Notepad" parent="Base.Theme.Notepad" >
        <item name="android:statusBarColor">@color/d_background</item>
        <item name="android:windowBackground">@color/d_background</item>

        <item name="android:fontFamily">@font/sarabun</item>

<!--        Custom colors-->
        <item name="cardColor">@color/d_card</item>

        <item name="faded_sm">@color/d_faded_sm</item>
        <item name="faded_md">@color/d_faded_md</item>
    </style>
</resources>